# Header + Variables for Challenge 1
step_count = 0
print("=== Challenge 1: Collatz Conjecture ===")
current_number = int(input("Enter starting number: "))
print("Sequence:", end=" ")

# Check if current number is not equal to 1
while current_number != 1:
    # Calculation for Evens
    if current_number % 2 == 0:
        print(current_number, end=" ")
        current_number = int(current_number / 2)
        step_count += 1
    # Calculation for Odds
    elif current_number % 2 == 1:
        print(current_number, end=" ")
        current_number = int(current_number * 3 + 1)
        step_count += 1
print(1, end=" ")
print("\nSteps:", step_count)
# I used a while loop instead of a for loop because I didn't know how many times I would run the loop

# Header + Variables for Challenge 2
print()
print("=== Challenge 2: Prime Number Checker ===")
prime_number = int(input("Enter a number: "))
print(f"Testing divisors from 2 to {prime_number - 1}...")
prime = True

# I used booleans and a for loops to logically print out is prime! for a prime number and is not prime + divisor for non-prime numbers
for i in range(2, int(prime_number) - 1):
    if prime_number % i == 0:
        print(f"{prime_number} is not prime (divisible by {i})")
        prime = False
        break
if prime == True:
    print(prime_number, "is prime!")
# I used a for loop because the iterations could be quantified through range()

# Header for Challenge 3
print()
print("=== Challenge 3: Multiplication Table ===")
print("Multiplication Table:")

# These print out the calculations from 1-10 in a times table format

# This is for the header part
for i in range(1, 11):
    if i == 1:
        print(f"  {i:4}", end="")
    else:
        print(f"{i:5}", end="")

# This is for the 10x10 table
for row in range(1, 11):
    print(f"\n{row:3}", end="")
    for product in range(1,11):
        print(f"{product*row:4}", end="")
        print(" ", end="")
# Nested loops are important because they stop you from having to repeat the same lines of codes many times over. The outer loop should set up a base for the inner loop. In this problem I used the outer loop to make the rows and the inner to do the calculations.
